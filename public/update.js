/*const mod = require('./public/main');
var val = mod.getValue();

document.getElementById("inLastName").value = updateContent.lastName;
document.getElementById("inFirstName").value = updateContent.firstName;
document.getElementById("inBirthDate").value = updateContent.birthDate;
document.getElementById("inEMail").value = updateContent.email;
document.getElementById("inAdress").value = updateContent.adress;
document.getElementById("inSubscriptionEnd").value = updateContent.subscriptionEnd;*/

/*var update = document.addEventListener('update');
update.addEventListener('click', function () {
    fetch('subscriptions', {
        method: 'put',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify(updateContent)
    }).then(function(res) {
        if (res.ok) return res.json();
    }).then(function(data) {
        console.log(data);
        window.location.reload(true);
    });
});*/